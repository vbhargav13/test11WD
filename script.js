document.addEventListener('DOMContentLoaded', function() { const form = document.getElementById('loginForm'); if (form) { form.addEventListener('submit', function(e) { e.preventDefault(); const name = document.getElementById('loginName').value; localStorage.setItem('userName', name); window.location.href = 'home.html'; }); } const nameSpan = document.getElementById('welcomeName'); if (nameSpan) { nameSpan.textContent = localStorage.getItem('userName'); } const quizForm = document.getElementById('quizForm'); if (quizForm) { quizForm.addEventListener('submit', function(e) { e.preventDefault(); let score = 0; if (document.querySelector("input[name='q1']:checked")?.value === 'correct') score++; document.getElementById('quizResult').textContent = `You scored ${score}`; }); } const fbForm = document.getElementById('feedbackForm'); if (fbForm) { fbForm.addEventListener('submit', function(e) { e.preventDefault(); const name = document.getElementById('fbName').value; const topic = document.getElementById('fbTopic').value; const message = document.getElementById('fbMessage').value; localStorage.setItem('feedback', JSON.stringify({ name, topic, message })); document.getElementById('feedbackDisplay').textContent = `Thank you, ${name}. Feedback received.`; fbForm.reset(); }); } });